import Title from '@/components/Title';

export default function About() {
  return (
    <section
      id="about"
      className="from-sec-gra-start to-background flex min-h-[calc(100dvh-var(--header-h))] w-auto scroll-mt-[var(--header-h)] justify-center bg-linear-to-br"
    >
      <Title>About Me</Title>
    </section>
  );
}
