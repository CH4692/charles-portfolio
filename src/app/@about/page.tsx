import Title from '@/components/Title';

export default function About() {
  return (
    <section
      id="about"
      className="from-sec-gra-start to-background bg-linear-to-brz flex min-h-[calc(100dvh-var(--header-h))] w-full scroll-mt-[var(--header-h)] justify-center"
    >
      <Title>About Me</Title>
    </section>
  );
}
